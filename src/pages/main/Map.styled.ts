import styled from 'styled-components';

export const mapWrapper = styled.div`
  width: 100vw;
  height: 95vh;
  position: flex;

  button {
    position: flex;
    height: 20px;
    z-index: 500;
  }
`;
