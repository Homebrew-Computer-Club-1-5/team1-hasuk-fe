import HouseInfos from './HouseInfos';
import styled from 'styled-components';
const HouseWrapper_OnlyInfos = styled(HouseInfos)`
  margin: 10px 0px; // 적용 안되노;;
`;

export default HouseWrapper_OnlyInfos;
